<%= form_with(model: kitten, local: true) do |form| %>
  <% if kitten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kitten.errors.count, "error") %> prohibited this kitten from being saved:</h2>

      <ul>
      <% kitten.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.label :name, 'Name:' %>
    <%= form.text_field :name %>
    <%= form.label :age, 'Age:' %>
    <%= form.select :age, (1..25) %>
    <%= form.label :cuteness, 'Cuteness:' %>
    <%= form.select :cuteness, (1..10) %>
    <%= form.label :softeness, 'Softness:' %>
    <%= form.select :softness, (1..10) %><br />
    <%= form.submit %>
  </div>
<% end %>
